# Drop-in: ensure nginx starts after backends after power restore.
# Installed to: /etc/systemd/system/nginx.service.d/banana-apps.conf

[Unit]
After=network-online.target clock-terminal.service weighbridge.service
Wants=clock-terminal.service weighbridge.service
